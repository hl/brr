version: "2"
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
  exclusions:
    rules:
      # Test helpers — unchecked os calls in test setup are fine
      - path: _test\.go
        linters: [errcheck]
